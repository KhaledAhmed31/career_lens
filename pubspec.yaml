name: career_lens
description: "Career Lens, your guide to career success."
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  device_preview: ^1.3.1
  equatable: ^2.0.8
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  flutter_slidable: ^4.0.3
  get_it: ^9.2.1
  go_router: ^17.2.2
  injectable: ^3.0.0
  shared_preferences: ^2.5.5

dev_dependencies:
  build_runner: ^2.14.1
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  injectable_generator: ^3.0.2
flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/json/
